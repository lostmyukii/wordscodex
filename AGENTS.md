# 智能词汇学习平台开发约定

## 沟通约定

- 与用户交流、进度汇报、技术说明和文档默认使用中文。
- 代码标识符、提交中的技术名词和 API 字段使用英文。
- 遇到需求与工程约束冲突时，直接说明冲突、影响和推荐方案，不默默猜测。

## 项目简介

本项目是一个面向 K12、大学考试、考研/出国考试和职场用户的智能词汇学习平台。

MVP 使用 **React + TypeScript 响应式 Web/PWA**，核心闭环是：

```text
选择词库 → 生成计划 → 学习新词 → 主动回忆 → SRS 复习
→ 错词强化 → 学习反馈 → 打卡
```

产品需求见 [`融合版_智能词汇学习产品需求分析.md`](融合版_智能词汇学习产品需求分析.md)，工程方案见 [`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md)。

## 事实来源优先级

出现不一致时，按以下顺序执行：

1. 用户在当前任务中的明确要求；
2. `docs/DEVELOPMENT.md` 中的 MVP 范围、数据规则和验收标准；
3. `融合版_智能词汇学习产品需求分析.md` 中的产品方向；
4. 已有代码、测试和接口约定；
5. 本文件中的通用开发约定。

不能自行把需求分析中的 P1/P2 功能提升为 MVP 必做项。

## 技术基线

- pnpm workspace
- React + TypeScript + Vite
- React Router
- TanStack Query
- Zustand
- React Hook Form + Zod
- Tailwind CSS
- vite-plugin-pwa + Workbox
- IndexedDB + Dexie
- Node.js + TypeScript + Fastify
- Prisma + PostgreSQL
- Redis
- Vitest + Testing Library + Playwright
- ESLint + Prettier

创建项目时使用稳定版本并锁定依赖。不要擅自替换核心框架或并行引入同类库。

## 预期项目结构

```text
apps/
├── web/                  # React Web/PWA
└── api/                  # Fastify REST API
packages/
├── contracts/            # 前后端共享 DTO 与 Zod Schema
├── domain/               # SRS、掌握度和计划等纯领域逻辑
└── config/               # 共享 TypeScript/ESLint 配置
scripts/
└── import-vocabulary/    # 词库导入与校验
docs/
└── DEVELOPMENT.md
```

按业务能力组织代码，避免把所有组件、接口或工具函数堆入单一大文件。

## 常用命令约定

根目录脚本建立后应保持以下统一入口：

```bash
pnpm install
pnpm dev
pnpm dev:web
pnpm dev:api
pnpm lint
pnpm typecheck
pnpm test
pnpm test:e2e
pnpm build
pnpm db:migrate
pnpm db:seed
pnpm vocabulary:validate
pnpm vocabulary:import
```

如果实际脚本与以上不同，必须同步更新 `README.md`、CI 和本文件。

## 开发原则

### 1. 先完成纵向闭环

优先做出一条可运行、可测试、可持久化的完整流程，再扩展更多页面。

推荐顺序：

1. 工程基线；
2. 登录和词库；
3. 学习计划；
4. 新词学习与主动回忆；
5. SRS 和错词；
6. 看板、打卡和 PWA；
7. 上线准备。

不要先搭建复杂权限平台、通用工作流引擎或微服务。

### 2. 主动回忆不能退化

- 学习流程必须包含作答；
- “看过卡片”不能直接视为掌握；
- 正误、耗时、题型和回答等级必须进入学习记录；
- 错词必须影响复习优先级；
- 页面动画和奖励不能阻断学习提交。

### 3. 领域逻辑与 UI 分离

以下逻辑放在 `packages/domain`，写成纯函数并优先测试：

- SRS 间隔计算；
- 掌握状态转换；
- 今日任务排序；
- 学习计划日期计算；
- 连续打卡计算；
- 离线操作去重。

React 组件不得自行计算另一套复习规则。客户端展示服务端返回的最终调度结果。

### 4. 服务端是最终事实来源

- 学习进度、打卡和计划以服务端数据为准；
- IndexedDB 只保存缓存、未完成会话和待同步操作；
- 本地完成但未同步时必须明确显示“待同步”；
- 写请求使用幂等键，重试不能重复累计学习次数；
- 学习记录写入和进度更新放在同一个事务中。

### 5. 不为未验证功能过度设计

MVP 不实现复杂社交、支付、教师端、AI 对话、发音评分或完整 FSRS。

允许定义清晰的模块边界，不允许创建：

- 空壳导航入口；
- 永远返回 mock 的正式 API；
- 未被当前需求使用的插件系统；
- 为假设中的多租户或微服务增加的抽象层；
- 同时存在的两套状态管理、请求库或表单方案。

## TypeScript 约定

- 启用 `strict`；
- 禁止在核心业务代码中使用 `any`，外部未知数据先用 `unknown` 并校验；
- 类型优先从 Zod Schema 推导，避免手写两份不一致的 DTO；
- 使用字符串联合类型表达有限状态；
- 对数据库时间统一使用 UTC ISO 8601 字符串传输；
- 金额、计数和时长必须在名称中体现单位，如 `responseMs`、`intervalDays`；
- 不使用 TypeScript `enum` 承载跨接口协议值，使用 `as const` 或字符串联合。

## React 约定

- 页面放在对应 `features` 模块中，路由层只负责组合和守卫；
- 服务器状态使用 TanStack Query，不复制进 Zustand；
- Zustand 只保存跨组件的短期客户端状态，如当前学习会话和离线队列；
- 表单统一使用 React Hook Form + Zod；
- 组件保持单一职责，复杂页面拆分为页面容器、业务组件和纯展示组件；
- 不在 `useEffect` 中复制可由 props、query 或选择器直接推导的状态；
- 所有异步页面必须提供加载、空数据、错误和重试状态；
- 列表项使用稳定业务 ID，不使用数组下标作为可变列表 key。

## 样式与交互约定

- 移动端优先，以 375px 宽度作为主要检查视口；
- 学习内容桌面端最大宽度建议为 720px；
- 点击区域不小于 44×44px；
- 正误反馈不能只依赖红绿颜色；
- 动画必须尊重 `prefers-reduced-motion`；
- 不使用悬停作为唯一操作方式；
- 拼写题在手机软键盘弹出时仍能看到输入框与提交按钮；
- 设计令牌集中管理，禁止在各页面复制相近但不同的颜色和间距值。

## API 约定

- 路径前缀统一为 `/api/v1`；
- 请求和响应通过 `packages/contracts` 中的 Schema 校验；
- 错误使用稳定业务代码和面向用户的中文消息；
- 不暴露 SQL、堆栈、内部路径或第三方密钥；
- 列表默认使用游标分页；
- 写入学习记录必须携带 `Idempotency-Key`；
- API 权限测试必须包含“用户不能读取或修改他人资源”；
- 修改接口时同步更新 contracts、调用方、测试和开发文档。

标准错误结构：

```json
{
  "error": {
    "code": "VALIDATION_FAILED",
    "message": "提交内容不完整，请检查后重试。",
    "requestId": "req_123"
  }
}
```

## 数据与 SRS 红线

- `ReviewLog` 只追加，不通过覆盖历史记录修正当前状态；
- `ReviewLog.idempotencyKey` 必须唯一；
- `nextReviewAt` 使用 UTC 保存；
- 一个用户同时只能有一个活动学习计划；
- 已掌握单词答错后必须进入 `lapsed` 或错词链路；
- `easeFactor` 限制在 `1.3` 到 `3.0`；
- 完整 SRS v1 规则以 `docs/DEVELOPMENT.md` 第 8 章为准；
- 任何算法调整必须包含测试、迁移影响说明和指标观察方案。

## PWA 与离线约定

- Service Worker 更新不能静默破坏进行中的学习会话；
- 当前会话写入 IndexedDB 后才允许显示为可离线继续；
- 待同步记录保留幂等键、创建时间、重试次数和最后错误；
- 鉴权失效时暂停同步，不能无限重试；
- 缓存图片和音频必须设置容量与过期策略；
- 不缓存包含敏感信息的认证响应；
- 不把“写入本地队列”展示为“服务端同步成功”。

## 内容约定

- 词库通过 `scripts/import-vocabulary` 导入，不直接修改生产数据库；
- 每个单词至少包含 lemma、词性和一个中文释义；
- 音频、图片和例句必须有可追踪来源；
- 同一词库内不允许重复 lemma；
- 导入操作必须先验证，再输出新增、更新、跳过和失败统计；
- 内容字段变化时更新版本号，不能破坏已有学习记录。

## 测试要求

### 必测领域逻辑

- SRS 四种回答等级；
- `easeFactor` 上下界；
- 新词、学习中、模糊、错词、掌握和遗忘状态转换；
- 到期复习排序；
- 计划完成日期；
- 跨时区打卡；
- 离线队列幂等同步。

### 必测用户路径

- 新用户完成引导与首次学习；
- 用户完成到期复习并打卡；
- 答错后进入错词本并完成强化；
- 刷新后恢复进行中的会话；
- 断网作答后恢复网络同步；
- 手机视口完成拼写题。

修复缺陷时先增加能复现问题的测试，再修改实现。纯文案和样式微调可不强制测试，但必须进行对应页面检查。

## 性能与安全

- 首页 LCP 目标小于 2.5 秒；
- 学习卡片交互响应目标小于 200ms；
- 非首屏路由懒加载；
- 不一次性加载完整大型词库；
- 验证码、登录和作答提交接口必须限流；
- 刷新令牌仅放入 HttpOnly、Secure Cookie；
- 密钥不得进入代码、日志或提交记录；
- 日志不得包含验证码、令牌、完整手机号或邮箱；
- 用户内容和错误信息在渲染前进行安全处理。

## 修改文档的时机

以下变化必须同步更新 `docs/DEVELOPMENT.md`：

- MVP 范围变化；
- 技术栈或目录结构变化；
- 核心实体、状态或 API 变化；
- SRS、掌握度或打卡规则变化；
- 离线同步边界变化；
- 性能、安全或验收标准变化。

不要让文档长期描述已经不存在的实现。

## 提交与变更范围

- 每次变更聚焦一个明确目标；
- 不在功能提交中混入无关重构和依赖升级；
- 不修改用户未要求且与任务无关的文件；
- 删除废弃实现，不长期保留注释代码；
- 提交前至少运行与变更相关的 lint、类型检查和测试；
- 宣称任务完成前必须报告实际执行过的验证命令和结果；
- 未运行的检查必须明确说明，不能推测其已通过。

## 完成检查

提交任务前确认：

- 功能符合 MVP 范围；
- 数据和 API 类型一致；
- 加载、空状态、错误和重试已处理；
- 手机视口可正常操作；
- 必要测试已添加并通过；
- Lint、类型检查和构建未被破坏；
- 没有新增敏感信息、无来源内容或无效入口；
- 相关工程文档已经同步。
